
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Netlify Next.js runtime supports next/image. If needed, you can set
    // unoptimized: true to skip optimization.
  }
}
module.exports = nextConfig
